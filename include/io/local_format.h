#ifndef LOCAL_FORMAT_H
#define LOCAL_FORMAT_H

#include <vector>
#include <tuple>

// 定义一个结构体来存储两个二维数组
struct ArrayPair {
    std::vector<std::vector<int>> array1;
    std::vector<std::vector<int>> array2;
};

#endif // LOCAL_FORMAT_H
